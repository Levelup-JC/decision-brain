# Plan-IV 对接消息 — 复制粘贴给三位新员工

> 用法：把下面对应小节整段复制，分别发给 A / B / C 三位。每段都自带"读哪个文件、做哪几条、交什么证据"，他们不需要看历史记录。

---

## 发给 C 组（git 收口 + 录制环境演练，关键路径起点，先开工）

你好，你接替的是上一轮的 C 组。请先打开这份文件，它是本阶段唯一依据，自成一体：

`/Users/jasoncong/Desktop/Decision Brain/plan/Plan-IV-收尾提交作战计划.md`

读 §0 背景 + §1 现状（看清楚哪些已做实、哪三个缺口没关），然后做 **§3 的 C 组任务卡**：C-IV-1 到 C-IV-4。

你的活：
1. 把 `源代码/.gitignore` 那一笔遗留改动 commit + push，让 `git status` 真干净（只 add 这一个文件，别 `git add .`）。
2. 再做一次密钥泄漏复查。
3. 线上三路探活（health / `/` / state 全 200）——这是给 A/B 解依赖的信号，你探活过了他们才开工。
4. 录制窗口演练：确认一次连续会话内不触发 KV 冷启动丢数据。

**交付纪律（重要）**：按 §5 的 TC-IV 自测表做完，把每条的命令输出/截图贴进你的任务汇报。把控人会**亲手复跑你的声称**，对不上会打回。红线和审查纪律看 §6。

你是关键路径起点，C-IV-3 探活完成请通知 A/B 可以上。

---

## 发给 A 组（提交说明终稿 + 表单三链接，依赖 C 探活）

你好，你接替的是上一轮的 A 组。请先打开这份文件，它是本阶段唯一依据，自成一体：

`/Users/jasoncong/Desktop/Decision Brain/plan/Plan-IV-收尾提交作战计划.md`

读 §0 背景 + §1 现状，然后做 **§3 的 A 组任务卡**：A-IV-1 到 A-IV-4。等 C 组通知"线上探活通过"再开工。

你的活：
1. 线上 LLM 复跑一次，自己确认没退化（`degraded=false`、7 Agent、reply 是自然语言）。
2. 填 `plan/提交-Project-Description.md` §4 三链接：Demo URL、GitHub 先填；**Demo video 链接要等把控人主持 E2E 录完才有，先标"待 E2E 后回填"，别瞎填假链接**。
3. 提交说明终稿通读一遍：无"千问/Qwen"、DeepSeek 信息正确、Bitget 5 Skill 名字写全。
4. 降级保险线上复核（演示时别翻车）。

**交付纪律（重要）**：按 §5 的 TA-IV 自测表做完贴证据。说明点名 DeepSeek 就必须线上真跑过 DeepSeek。把控人会亲手复跑你的关键声称。红线和审查纪律看 §6。

---

## 发给 B 组（真人浏览器线上复验，依赖 C 探活）

你好，你接替的是上一轮的 B 组。请先打开这份文件，它是本阶段唯一依据，自成一体：

`/Users/jasoncong/Desktop/Decision Brain/plan/Plan-IV-收尾提交作战计划.md`

读 §0 背景 + §1 现状，然后做 **§3 的 B 组任务卡**：B-IV-1 到 B-IV-5。等 C 组通知"线上探活通过"再开工。

你的活：用**真人浏览器**打开公网 URL `https://decision-brain-gray.vercel.app`，肉眼看 + 截图为证，验五件事：
1. 线上真连接（顶部"已连接"、F12 控制台无 error）。
2. 委员会并发冒泡（7 Agent 逐个亮起，不是一次性刷出）。
3. 资产看板动画（count-up）。
4. 诚实标注（空字段灰标"待补充"，无 null/undefined）。
5. 降级提示（"规则模式"金标 / "LIVE"绿标）。

**交付纪律（最重要，上一轮就栽在这）**：这次不收"预期无 error""代码审查显示"，必须是真人浏览器实跑出来的**截图**，而且截图要能看到地址栏是 `decision-brain-gray.vercel.app`（证明是公网不是 localhost）。按 §5 的 TB-IV 自测表做完贴截图。红线和审查纪律看 §6。

---

## 三组都绿之后（把控人自己执行）

A/B/C 三组任务汇报都交齐、把控人复跑核对通过后，把控人主持 **§3 All 的线上 E2E 9 步**（公网 URL、三组在场、全程录屏），录屏剪 ≤3 分钟回填给 A 组填进 Demo video 链接，最后对照 **§7 终审清单 13 项**全绿，正式提交。
